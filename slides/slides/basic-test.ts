describe("a test suite / SUT", () => {

    beforeAll(() => {})
    beforeEach(() => {})

    it("should validate the behaviour", () => {
        // actual test code, assertions, etc
    })

    afterEach(() => {})
    afterAll(() => {})

})