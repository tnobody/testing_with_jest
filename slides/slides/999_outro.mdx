import {Gyphy} from '../components/gyphy'
import {Appear} from 'mdx-deck'
import {CodeSlide} from '../components/code-slide'

<CodeSlide lang="typescript" code={`
afterAll(() => {
    expect(speaker).toSay('thank you for listening')
})
`.trim()} 
    highlights={[{line:[0,1,2]}]}
/>


<Appear>
    <div><Gyphy id="3oz8xIsloV7zOmt81G" height="200" /></div>
    <h3>Now get some 🍕 and 🍺</h3>
</Appear>